from autobreadcrumbs import site
from django.utils.translation import ugettext_lazy

site.update({
    'demand-public-add': ugettext_lazy('consult us'),
    'batcave-index': ugettext_lazy('batcave'),
    'batcave-structure-list': ugettext_lazy('structure list'),
})
